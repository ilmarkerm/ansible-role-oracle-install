# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

# User specific aliases and functions

{% for item in oracle_install_os_alias %}
alias {{ item.alias }}='{{ item.target }}'
{% endfor %}
